# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# --- Database Connection URL ---
# Choose ONE and uncomment it, or add your own.
# Make sure the database provider you uncomment here matches the 'provider' uncommented in prisma/schema.prisma

# SQLite (DEFAULT: simplest local setup, uses a file in this project directory)
DATABASE_URL="file:./dev.db"

# PostgreSQL
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
# Example: DATABASE_URL="postgresql://postgres:mysecretpassword@localhost:5432/prisma_dev?schema=public"

# MySQL
# DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"
# Example: DATABASE_URL="mysql://root:mysecretpassword@localhost:3306/prisma_dev"

# SQL Server
# DATABASE_URL="sqlserver://HOST:PORT;database=DATABASE;user=USER;password=PASSWORD;encrypt=true"
# Example: DATABASE_URL="sqlserver://localhost:1433;database=prisma_dev;user=sa;password=myStrong(!)Password;encrypt=false"

# MongoDB
# DATABASE_URL="mongodb+srv://USER:PASSWORD@HOST/DATABASE?retryWrites=true&w=majority"
# Example: DATABASE_URL="mongodb+srv://testuser:testpassword@cluster0.mongodb.net/prisma_dev?retryWrites=true&w=majority"

# --- Other Environment Variables (if needed) ---
# e.g., API keys, external service URLs
# API_KEY="your_secret_api_key"

